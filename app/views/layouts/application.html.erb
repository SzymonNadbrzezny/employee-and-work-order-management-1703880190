<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Employee And Work Order Management" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>
    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body class="bg-slate-50">
    <nav class="w-full p-2 h-[50px] bg-slate-700 grid grid-cols-2 md:grid-cols-3 text-slate-300 shadow-sm">
      <span class="flex items-center">
        E&W Management
      </span>
      <div class="flex items-center justify-center">
        <div class="px-4  bg-slate-600 hover:bg-slate-400 hover:text-slate-700 rounded-full transition-colors">Nawigacja</div>
      </div>
      <span class="hidden md:flex items-end "></span>
    </nav>
    <main class="w-full h-full mx-auto px-5 flex">
      <%= yield %>
    </main>
    <footer class="h-[50] w-full grid grid-cols-2 md:grid-cols-3 items-center bottom-0 absolute p-2 shadow-sm border-t-2 border-slate-300 bg-slate-100">
      <span class="hidden md:flex items-center">
      </span>
      <span class="flex items-center justify-center">
        © Szymon Nadbrzeżny <%= DateTime.now().year()%>
      </span>
      <span class="flex items-center justify-end">
        <%= link_to image_tag("github-mark.png",class:"aspect-square", size: "32x32", alt: "Github link"),"https://github.com/SzymonNadbrzezny/employee-and-work-order-management-1703880190/tree/main"%>
      </span>
    </footer>
  </body>
</html>
